<div class="col s3">
     <div class="detailstitle">
           <h2 class= "flow-text center-align">New Hour</h2>
     </div>
</div>

<%= render 'form' %>

<div class="fixed-action-btn back-button">
<%= link_to 'Back', Venue.where(owner: current_user.id).first, class: "waves-effect waves-light btn", 'data-no-turbolink' => true  %>
</div>
