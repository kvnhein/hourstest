 $(function(){
        $('#form-hide<%=@claim.event_id%>').click(function() {
            $(this).hide();
        });
    
$(".new_claim<%=@claim.event_id%>").show()
$(".new_claim<%=@claim.event_id%>").html("<%= escape_javascript(render @claim) %>")
$("#form_button<%=@claim.event_id%>").hide()
$(".verify_button<%=@claim.event_id%>").hide()
});