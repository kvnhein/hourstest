<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @claim.user %>
</p>

<p>
  <strong>Event:</strong>
  <%= @claim.event %>
</p>

<%= link_to 'Edit', edit_claim_path(@claim) %> |
<%= link_to 'Back', claims_path %>
