$('#claim-up<%=@claim.id%>').bind('ajax:success', function() {
    $('#up_count<%=@claim.id%>').html('<%=escape_javascript @claim.get_upvotes.size.to_s %>');
    $('#claim-up<%=@claim.id%>').html('<%=escape_javascript (render "claim_upvote") %>');
    });