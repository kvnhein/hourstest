<p id="notice"><%= notice %></p>


<div class="col s3">
     <div class="detailstitle">
           <h2 class= "flow-text center-align">Venues</h2>
     </div>
</div>

<div class="row" style="margin0;">
    <div class="col s12 profile-tabs">
      <ul class="tabs tab-demo" style="width: 100%;">
        <li class="tab col s3"><a class="active" href="#Market_Square">Market Square</a></li>
        <li class="tab col s3"><a href="#Southside">South Side</a></li>
        <li class="tab col s3"><a href="#Shadyside">Shadyside</a></li>
         <li class="tab col s3"><a href="#Oakland">Oakland</a></li>
      </ul>
    </div>
  </div>

<script>
   $(document).ready(function(){
    $('ul.tabs').tabs();
  });
</script>

<div id="Market_Square">
  <% @venues.where(neighborhood_id: 5).each do |venue| %>
 <div class="row hourslist">
        <div class="col s12">
          <div class="card beerlist">
            <div class="card-content special-by-day">
              <h4 class="venues"><%= venue.name %></h4>

          <div class="timeforspecial streets">
              <%= truncate(venue.address, length: 30) %><br/>
              <%=Neighborhood.where(id: venue.neighborhood_id).first.name %>
              </div>
            </div>

              <%= link_to 'Show', venue, class: "activator waves-effect waves-light btn openhours", 'data-no-turbolink' => true  %>


            <div class="arrow">
                <%= image_tag "arrow.png"%>
              </div>
          </div>
        </div>
      </div>
   <% end %>
</div>

<div id="Southside">
  <% @venues.where(neighborhood_id: 1).each do |venue| %>
 <div class="row hourslist">
        <div class="col s12">
          <div class="card beerlist">
            <div class="card-content special-by-day">
              <h4 class="venues"><%= venue.name %></h4>

          <div class="timeforspecial streets">
              <%= truncate(venue.address, length: 30) %><br/>
              <%=Neighborhood.where(id: venue.neighborhood_id).first.name %>
              </div>
            </div>

              <%= link_to 'Show', venue, class: "activator waves-effect waves-light btn openhours", 'data-no-turbolink' => true  %>


            <div class="arrow">
                <%= image_tag "arrow.png"%>
              </div>
          </div>
        </div>
      </div>
   <% end %>
</div>

<div id="Shadyside">
 <% @venues.where(neighborhood_id: 2).each do |venue| %>
 <div class="row hourslist">
        <div class="col s12">
          <div class="card beerlist">
            <div class="card-content special-by-day">
              <h4 class="venues"><%= venue.name %></h4>

          <div class="timeforspecial streets">
              <%= truncate(venue.address, length: 30) %><br/>
              <%=Neighborhood.where(id: venue.neighborhood_id).first.name %>
              </div>
            </div>

              <%= link_to 'Show', venue, class: "activator waves-effect waves-light btn openhours", 'data-no-turbolink' => true  %>


            <div class="arrow">
                <%= image_tag "arrow.png"%>
              </div>
          </div>
        </div>
      </div>
   <% end %>
</div>


<div id="Oakland">
 <% @venues.where(neighborhood_id: 3).each do |venue| %>
 <div class="row hourslist">
        <div class="col s12">
          <div class="card beerlist">
            <div class="card-content special-by-day">
              <h4 class="venues"><%= venue.name %></h4>

          <div class="timeforspecial streets">
              <%= truncate(venue.address, length: 30) %><br/>
              <%=Neighborhood.where(id: venue.neighborhood_id).first.name %>
              </div>
            </div>

              <%= link_to 'Show', venue, class: "activator waves-effect waves-light btn openhours", 'data-no-turbolink' => true  %>


            <div class="arrow">
                <%= image_tag "arrow.png"%>
              </div>
          </div>
        </div>
      </div>
   <% end %>
</div>

