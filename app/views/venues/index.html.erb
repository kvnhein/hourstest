<p id="notice"><%= notice %></p>


<div class="col s3">
     <div class="detailstitle">
           <h2 class= "flow-text center-align">Venues</h2>

     </div>
</div>

<div class="row" style="margin:0;">
    <div class="col s12 profile-tabs venues">
      <ul class="tabs tab-demo" style="width: 100%;">
        <li class="tab"><a class="active" href="#Market_Square">Downtown</a></li>
        <li class="tab"><a href="#Southside">South Side</a></li>
        <li class="tab"><a href="#Shadyside">Shadyside</a></li>
         <li class="tab"><a href="#Oakland">Oakland</a></li>
        <li class="tab"><a href="#Lawrenceville">Lawrenceville</a></li>
        <li class="tab"><a href="#Strip_district">Strip District</a></li>
      </ul>
    </div>
  </div>

<script>
   $(document).ready(function(){
    $('ul.tabs').tabs();
  });
</script>

<div id="Market_Square">
  <% @venues.where(neighborhood_id: 5).order("name ASC").each do |venue| %>
     <%@venue = venue %>
     <%= render 'venue_index'%>
<% end %>
</div>

<div id="Southside">
<% @venues.where(neighborhood_id: 1).order("name ASC").each do |venue| %>
     <%@venue = venue %>
     <%= render 'venue_index'%>
<% end %>
</div>

<div id="Shadyside">
 <% @venues.where(neighborhood_id: 2).order("name ASC").each do |venue| %>
     <%@venue = venue %>
     <%= render 'venue_index'%>
<% end %>
</div>


<div id="Oakland">
<% @venues.where(neighborhood_id: 3).order("name ASC").each do |venue| %>
     <%@venue = venue %>
     <%= render 'venue_index'%>
<% end %>
</div>


<div id="Lawrenceville">
 <% @venues.where(neighborhood_id: 7).order("name ASC").each do |venue| %>
     <%@venue = venue %>
     <%= render 'venue_index'%>
<% end %>
</div>

<div id="Strip_district">
 <% @venues.where(neighborhood_id: 3).order("name ASC").each do |venue| %>
     <%@venue = venue %>
     <%= render 'venue_index'%>
<% end %>
</div>

